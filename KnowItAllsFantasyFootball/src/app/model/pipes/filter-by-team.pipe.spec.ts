import { FilterByTeamPipe } from './filter-by-team.pipe';

describe('FilterByTeamPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByTeamPipe();
    expect(pipe).toBeTruthy();
  });
});
